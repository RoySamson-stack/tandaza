version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always
    # Optional: Add environment variables if needed
    # environment:
    #   - NODE_ENV=production